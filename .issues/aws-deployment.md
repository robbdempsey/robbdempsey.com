# Feature Overview

We have an AWS account which is serving the website in this directory. There needs to be a way to deploy from github to multiple environment based deployments. (ie. dev, prod)

## Your job

- write a plan that outlines the proper AWS resources for implementing a staged deployment. only write the plan for the DEV stage.
- write a plan for the github workflow
- secrets should be stored safely in github. HOW that is done should be outlined in your plan.
- if you need information about AWS or github ask.